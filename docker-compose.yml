version: "3"
services:
  db:
    container_name: postgres
    image: postgres
    environment:
      POSTGRES_DB: db123
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: dbpassword
    ports:
      - "5432:5432"